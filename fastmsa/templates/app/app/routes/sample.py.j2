from fastmsa.api import get

from fastmsa.repo import SqlAlchemyRepository
from {{msa.name}}.domain.models import User

@get('/users')
def read_users():
    with SqlAlchemyRepository(User) as users:
        return users.all()
